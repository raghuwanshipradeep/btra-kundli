services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - narrative_cache:/app/narrative_cache.db
    restart: unless-stopped

volumes:
  narrative_cache:
